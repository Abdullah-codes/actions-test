name: Experiment
on: [push]

jobs:
  job1:
    name: Job 1
    runs-on: ubuntu-latest

    steps:
    - uses: hashicorp/setup-terraform@v2
      with:
         terraform_version: v1.1.3
    - name: Cheking terraform version
      run: |
         terraform version
        

  job2:
    name: Job 2
    needs: job1
    runs-on: ubuntu-latest

    steps:
       - shell: bash
         env:
          SUPER_SECRET: ${{ secrets.TEST_ENV }}
         run: |
           echo "$SUPER_SECRET"
       - name: Dependant is Running
         run: |
            echo "Completed job 2, but triggering failure"
        
